syntax = "proto2";

package seo;

message Response {
  enum Status {
    OK = 0;
    CONNECTION_ERROR = 1;
  }

  optional string source_code = 1;
  optional Status status = 2;
  optional uint64 id = 3;
}
