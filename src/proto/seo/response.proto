syntax = "proto2";

package seo;

message Response {
  enum Status {
    OK = 0;
    CONNECTION_REFUSED = 1;
    NAME_NOT_RESOLVED = 2;
  }

  optional string source_code = 1;
  optional Status status = 2;
  optional uint64 id = 3;
}
